# uxdemo Agent Guide

本文件约束 `uxdemo/` 目录及其所有子目录内的 AI 修改行为。

---

## 工作区定位

- `uxdemo/` 是**设计评审用交互原型工作区**，不是主产品代码。
- 这里的交付物是**可直接打开的原型 HTML** 与其配套的**交互逻辑文档**。
- 目标是准确表达**交互流程、状态切换、异常分支与方案差异**，不是产出可复用的生产代码。

---

## 目录约定

- 一个设计点对应一个子文件夹，目录名使用**小写连字符**，例如 `skill-upload`、`agent-config-flow`
- 默认每个原型文件夹包含：
  - `prototype.html`
  - `interaction-spec.md`
- `templates/` 只作为模板来源，不承载具体原型内容
- 当前 `dora-new-architecture-design/` 属于已知例外，可保留多个原型 HTML

---

## 工作流程

- **够用就停**：优先基于需求、截图、现有原型修改；需要读源码时只读当前设计点相关文件。
- **判断前置**：涉及入口、流程、状态、页面结构时，先用 `uxdemo/skills/interaction-design/SKILL.md` 做快速判断；不要展开长篇复盘。
- **先判断，再动手**：在开始画页面前，必须先判断“这是什么类型的页面”，至少明确以下四件事：
  - 当前页面首先是**列表页 / 配置页 / 详情页 / 工作台 / 说明页**中的哪一种
  - 当前页面的**主任务**是什么，用户进来是查看、配置、分配、管理，还是阅读说明
  - 当前页面的**主对象**是什么，是用户、部门、角色、资源，还是权限项
  - 当前页面应优先套用哪种**成熟界面模型**，例如“后台列表管理”“左列表右详情”“角色权限配置”“Wizard”
- **先定骨架，再补细节**：未明确页面类型和主任务前，不得直接进入高保真内容设计；先出低保真结构，再补文案、状态和交互说明。
- **文档不是页面**：产品文档、权限说明、规则表格默认作为页面设计依据，而不是直接照搬成页面主体；必须先拆成“谁要管理”“管理什么”“在哪里操作”。
- **优先复用成熟后台模型**：遇到 `用户管理`、`权限管理`、`角色管理`、`资源管理` 这类典型后台场景时，默认先套用成熟管理界面心智模型，不要先自由发挥成说明页或概念页。
- **结构不确定时先停**：如果页面结构还存在明显不确定性，先给出 1-3 句话的结构判断或低保真方向，再继续实现；不要一边猜一边堆细节。
- **文档同步**：修改 `prototype.html` 时，必须**在同一次回复中**同步更新对应 `interaction-spec.md`；文档只写当前结果态。
- **经验沉淀**：设计判断按三层组织（原则 / 规范 / 决策），写入路径详见下方「决策沉淀协议」。
- **新增原型**：新增原型文件夹时，同步更新 `uxdemo/index.html` 和 `uxdemo/README.md`。

---

## 决策沉淀协议

设计判断按三层组织，写入路径与触发方式不同：

| 层 | 性质 | 文件位置 | 写入路径 |
|---|---|---|---|
| **原则 (principle)** | 帮你*判断*的元规则 | `skills/interaction-design/references/example-principles.md` | 用户发起"提炼回合"时更新 |
| **规范 (convention)** | 直接*执行*的交互套路 | `skills/interaction-design/references/conventions.md` | 用户口述成熟套路 → 直接写入 |
| **决策 (decision)** | 案例级判断*留痕* | 各原型文件夹下的 `decision-log.md` | 用户触发短语 → 按模板追加 |

### 触发短语

只有当用户使用下列短语时，才在当前原型文件夹下写入 `decision-log.md`：

- 「记进决策手册」
- 「提炼这个决策」

触发后必须**按 `templates/decision-log-template.md` 格式追加**，不可自由发挥字段。
不存在该文件则按模板创建。

### 提炼回合

由**用户主动发起**，不自动执行：

- 我扫描相关原型的 decision-log（重点看标记 `待提炼` 的条目）
- 提出"是否补一条原则到 example-principles.md" / "是否在 conventions.md 增加一条规范"
- 用户拍板后才落地，不自作主张修改原则/规范层

### 三层之间的关系

- **决策 → 原则**（慢路径）：多个 decision-log 命中同类判断 → 用户发起提炼 → 抽象成原则
- **直接 → 规范**（快路径）：成熟套路无需经过案例，用户口述即可写入
- **原则 ↔ 规范**：原则约束"怎么想"，规范约束"怎么做"；同一场景可能两层都涉及

---

## HTML 原型硬性约束

- 必须为**单文件自包含 HTML**
- 所有 CSS、JS 必须内联
- 禁止引入 CDN、外部脚本、外部样式、外部字体
- 图标必须使用在线开源图标库提供的现成图标（如 Lucide、Material Symbols、Iconify 等），可复制/内联其官方 SVG 以保持单文件自包含；禁止生成时自行绘制图标路径或临时设计自定义图标
- 字体使用系统字体栈
- 页面顶部必须保留**方案切换栏**
- 即使只有一个方案，也保留切换栏结构
- 关键状态必须可通过原型内 JS 演示，例如 loading、error、empty、disabled、success
- Mock 数据要有真实感，禁止使用明显占位文案

---

## 演示控件约束

- 所有不属于最终用户界面的辅助按钮、状态切换器、场景跳转器等，必须添加 `class="demo-control"`
- `.demo-control` 默认隐藏，只能通过切换栏中的“交互说明”开关显示
- 方案切换栏、交互说明入口、演示控件必须使用 fixed 浮层，不得占用文档流，不得给 `body` 或业务容器增加 padding / margin / top 偏移
- 演示控件应以浮动 popover 形式出现在“交互说明”入口附近，不得插入主内容区、挤压业务布局或遮挡核心操作路径
- 不要把演示控件混入正式业务区域视觉层级
- “交互说明”默认使用**目标容器边框高亮 + hover 说明气泡**模式，而不是固定说明面板或外置小圆点标号
- hover 说明气泡必须以**页面顶层浮层**渲染，不得依附在局部滚动区、卡片、弹窗、抽屉等可能裁切内容的容器内部
- 开启“交互说明”时，只高亮发生交互变更、入口变更、状态变更、方案差异的位置；关闭时高亮与说明必须完全不可见
- 开启“交互说明”前后，业务页面的原始布局、滚动位置、sticky/fixed 定位和可视区域必须保持一致；只允许叠加高亮、tooltip 和浮层控件
- 高亮对象应选择最能表达语义归属的容器：小图标高亮点击热区，大模块高亮语义容器，避免把说明定位到容器外侧
- 说明文案应简短描述“这里是什么设计”，不要写实现细节或长篇复盘
- 新建或重做原型时，优先复用 `uxdemo/templates/interaction-annotations.html` 中的标注结构与样式

---

## interaction-spec 文档约束

- 文档只记录：
  - 操作流程
  - 状态变化
  - 系统反馈
  - 异常分支
  - 方案差异
- 文档默认面向**设计评审结果**，应直接描述“当前设计是什么”
- 除非用户明确要求保留变更说明，否则不要写“原来 / 之前 / 不再 / 改为 / 从 A 变成 B”这类变更叙述
- 评审场景下，优先使用结果态表达，例如“入口位于对话页右上角”，不要写“入口不再放在侧边栏，而是放在右上角”
- 文档不要记录：
  - 颜色值
  - 像素尺寸
  - 圆角阴影
  - 非关键动效参数
- 结构默认遵循 `uxdemo/templates/interaction-spec-template.md`
- 文档分两层，评审优先看 L1：
  - **L1 — 评审摘要（`## 零`）**：每节只写「本次变更要点」+「关键流程图（精简版，只含主路径）」
  - **L2 — 完整规格（`## 一` 起）**：详细流程图（含异常分支）+ 操作反馈表
- 更新方式判断：
  - **小改动**（新增一条交互、修改某个反馈）→ 定点修改对应章节
  - **大改动**（结构重组、多个流程联动、旧交互被替代）→ 全量重写
  - 判断标准：读完文档能否独立复现原型所有交互？若需结合历史版本才能理解，说明需要全量刷新

---

## 与主项目隔离

- `uxdemo/` 内文件不得被主项目代码 `import`
- 不要把 `uxdemo/` 原型改造成 React、Vite、TypeScript 或其他构建产物
- 不要为了原型修改主项目业务代码，除非用户明确要求

---

## 修改前自检

在提交 `uxdemo/` 相关改动前，默认检查：

- 是否先完成了页面类型、主任务、主对象、界面模型这 4 项判断
- 是否先确定低保真结构，再进入高保真细节
- 原型 HTML 是否仍为单文件可打开形式
- `prototype.html` 与 `interaction-spec.md` 是否一致
- 新增原型时 `index.html` 与 `README.md` 是否已同步
- 演示控件是否都带有 `.demo-control`
- 方案切换栏和演示控件是否为浮层，是否没有改变 `body`、业务 header、主内容区的布局和定位
- 交互说明是否采用就地标号模式，标号是否只出现在关键变更点
- 是否误引入了外部资源或主项目依赖
- 用户若使用了「记进决策手册」「提炼这个决策」等触发短语，是否已按模板写入 `decision-log.md`

---

## 参考文档

- 交互判断 skill：`uxdemo/skills/interaction-design/SKILL.md`
- 原则层沉淀：`uxdemo/skills/interaction-design/references/example-principles.md`
- 规范层沉淀：`uxdemo/skills/interaction-design/references/conventions.md`
- 决策日志模板：`uxdemo/templates/decision-log-template.md`
- 交互文档模板：`uxdemo/templates/interaction-spec-template.md`
- 切换栏模板：`uxdemo/templates/variant-bar.html`
- 交互说明标注模板：`uxdemo/templates/interaction-annotations.html`
